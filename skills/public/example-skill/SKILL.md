---
name: example-skill
description: 示例 skill，用于验证本仓库的 skill 工具链（init/validate/package）与目录/链接约束是否生效。
---

# 目标

验证最小 skill 结构、frontmatter 解析、以及 `SKILL.md` 内的相对链接校验都能正常工作。

# 流程

1. 需要了解示例用途时，阅读 `references/example.md`。
2. 需要确定性自检时，运行 `scripts/hello.py`。

# 护栏

- 保持此 skill 足够小且不绑定任何业务域。
